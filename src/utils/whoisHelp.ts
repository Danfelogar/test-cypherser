import {API_WHO_IS} from '@env';
// import WhoisApi from 'whois-api-js';
// const client = new WhoisApi.Client(API_WHO_IS);

export const whoisHelp = async () => {
  // domain: string
  // return client.get('pokeapi.co/api/v2/')
  // .then(function (whoisHelp: any) {
  //   return console.log({whoisHelp})
  // })
  // .catch(function (error: any) {
  //   return console.log(error)
  // })
}